.env
# Backup .env = SECRET LEAK risk (contengono API key in chiaro). Mai committare.
.env.backup*
.env.*.backup
*.db
*.sqlite
# Backup/snapshot DB locali (non committare; bloat repo). db_snapshots/ è eccezione sotto.
*.db.backup*
*.db.bak*
*.bak
# Eccezione: gli snapshot in db_snapshots/ sono committati apposta (porting tests).
!db_snapshots/*.db
__pycache__/
*.pyc
.venv/
venv/
uploads/assets/
uploads/thumbnails/
data/capitolato_uploads/
.DS_Store
Thumbs.db
*.egg-info/
dist/
build/
.pytest_cache/
htmlcov/
.claude/recall-context.md
.claude/settings.local.json
.idea/
.vscode/
*.log

# Log temporanei copilot AI (incollati a mano da Matteo per debug)
docs/Logs-temp/

# File generati durante smoke test
q_test.*
q3.csv
q3.pdf
q3.xlsx

# graphify knowledge graph (α.172.29) — rigenerabile localmente con `graphify .`
graphify-out/
.graphify-cache/

# superpowers brainstorm visual companion (mockup HTML + state)
.superpowers/

# Google OAuth client secret (mai committare)
docs/google_api/
